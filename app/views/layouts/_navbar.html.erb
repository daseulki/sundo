<!-- Top Bar -->
<div class="u-header__section g-brd-bottom g-brd-gray-light-v4 g-py-18">
  <div class="container">
    <div class="row align-items-center">
      <!-- Logo -->
      <div class="col-md-3 g-hidden-md-down">
        <a href="/" class="navbar-brand">
          <img src="/assets/img/logo.png" alt="Logo">
        </a>
      </div>

      <!-- End Logo -->

      <!-- Subscribe Form -->
      <div class="col-6 col-md-6">
        <form action="/searchs" class="input-group rounded" >
          <input class="form-control w-80 g-brd-secondary-light-v2 g-brd-primary--focus g-color-secondary-dark-v1 g-placeholder-secondary-dark-v1 g-bg-white g-font-weight-400 g-font-size-13 rounded g-px-20 g-py-12" type="text" placeholder="오늘은 무엇을 찾아볼까요?" name = "q">
          <button class="btn u-btn-white g-color-primary--hover g-bg-secondary g-font-weight-600 g-font-size-13 text-uppercase rounded g-py-12 g-px-20" type="submit" >
            <i class=" fa fa-search"></i>
          </button>
        </form>
      </div>
      <!-- End Subscribe Form -->


      <!-- Account -->
      <div class="col-2">
        <% if user_signed_in? %>
        <p align = "right">
          <%= current_user.email %>
          <%= link_to('로그아웃', destroy_user_session_path, method: :delete)%>
        </p>

        <% else %>
        <p align = "right">
          <%= link_to('로그인', new_user_session_path) %>
          <%= link_to('회원가입', new_user_registration_path)%>
        </p>
        <% end %>

      </div>
      <!-- End Account -->
    </div>
  </div>
</div>
<!-- End Top Bar -->
