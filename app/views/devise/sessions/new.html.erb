<div class = "row" style="margin-top: 50px">
  <div class = "col-md-4 offset-md-4">
<h2 style="text-align:center" ><i class="fas fa-child"></i>   로그인을 환영합니다!   <i class="fas fa-child"></i></h2>
<br/><br/>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="form-inputs">
    <div class="input-group mb-3">
      <div class="input-group-prepend">
        <span class="input-group-text" id="basic-addon1">이메일</span>
      </div>
      <%= f.email_field :email, required: false, autofocus: true, class: "form-control" %>
    </div>
    <div class="input-group mb-3">
      <div class="input-group-prepend">
        <span class="input-group-text" id="basic-addon1">비밀번호</span>
      </div>
    <%= f.password_field :password, required: false, class: "form-control" %>
    </div>
      <p>아이디를 기억하시겠습니까?  <%= f.check_box :remember_me, as: :boolean if devise_mapping.rememberable? %></p>
  </div>

  <div class="form-actions">
    <%= f.submit "로그인", class: "btn btn btn-dark" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
  </div>
    </div>
